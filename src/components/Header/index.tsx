import React from "react";
import { Link } from "react-router-dom";

const Header = () => {
    return (
    <nav>
        <a>Войти</a>
        <a>К задачам</a>
    </nav>
    )
}

export default Header